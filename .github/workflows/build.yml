name: Build

# Runs only when a version tag (e.g. v0.1.0) is pushed.
# Builds installers for macOS (universal), Windows, and Linux, then
# publishes them to a GitHub Release for that tag.
on:
  push:
    tags: ['v*']

# tauri-action needs write access to create the Release and upload assets.
permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS is built locally via `npm run tauri build -- --target universal-apple-darwin`
          # to avoid the 10x GitHub-hosted macOS minute cost. Upload the .dmg to the
          # Release manually. Uncomment to restore CI macOS builds.
          # - platform: macos-latest
          #   args: '--target universal-apple-darwin'
          - platform: windows-latest
            args: ''
          - platform: ubuntu-22.04
            args: ''

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux deps
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt update && sudo apt install -y \
            libwebkit2gtk-4.1-dev libssl-dev \
            libayatana-appindicator3-dev librsvg2-dev libsecret-1-dev \
            gstreamer1.0-plugins-base gstreamer1.0-plugins-good \
            gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-libav
          # These GStreamer plugins must be present at build time so the
          # AppImage's bundleMediaFramework step can bundle them. They give the
          # bundled WebKitGTK its media pipeline (MSE, WebRTC via webrtcbin, and
          # H.264/HEVC software decode via libav) on host distros — e.g. Arch —
          # that ship a stripped WebKitGTK without these.

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install JS deps
        run: npm ci

      - name: Build + publish installers to a GitHub Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Supervision Client ${{ github.ref_name }}'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
